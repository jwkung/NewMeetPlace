<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/folder_orange_light_image</item>
        <item name="colorPrimaryDark">@color/folder_orange_light_image</item>
        <item name="colorAccent">@color/folder_orange_light_image</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NAVIGATION_BAR" parent="AppTheme" />

    <style name="MapTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/folder_orange_light_image</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/folder_orange_light_image</item>
        <item name="android:colorBackground">@color/L_header_text</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
        <item name="android:textColorPrimary">@color/nvtext</item>

    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/white_rice</item>
        <item name="colorPrimary">@color/folder_orange_light_image</item>
        <item name="colorAccent">@color/folder_orange_light_image</item>
        <item name="android:textColorPrimary">@color/infowindowtextcolor</item>
    </style>




</resources>
